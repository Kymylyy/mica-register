# ⚠️  SECURITY WARNING ⚠️
# NEVER commit the .env file to git!
# This file contains sensitive data (API keys, passwords, secrets)
# The .env file is already in .gitignore - keep it that way!

# Database Configuration
# Uncomment and configure for PostgreSQL, or leave commented to use SQLite (local dev only)

# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://postgres:password@localhost:5432/mica_register

# CORS Origins (comma-separated for production)
# Example: CORS_ORIGINS=https://your-app.vercel.app,https://www.yourdomain.com
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Admin API token for protected import endpoints:
# - POST /api/admin/import
# - POST /api/admin/import-all
# Use a strong random value in production.
# ADMIN_API_TOKEN=replace_with_strong_random_token

# For production, set these environment variables in your hosting platform
# Examples:
# - Railway: Set in project settings
# - Render: Set in environment variables
# - Heroku: Set via heroku config:set DATABASE_URL=...

# LLM Remediation (REQUIRED for LLM-based data cleaning)
# Get your API key from: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=your_deepseek_api_key_here
